import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Bikoret | Blockchain Analytics | Deciphering DEX data for smart crypto investments',
  description: 'Deciphering DEX data for smart crypto investments. Blockchain analytics. Find the best crypto investments sooner.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
